# VideoPicker_iOS
iOS版動画切り抜きアプリ

## OpenCV setup (local only)

This project depends on OpenCV xcframework, which is NOT committed.

Place:
ios/VideoPickerScoring/ThirdParty/opencv2.xcframework

How to build:
- Build OpenCV for iOS (device + simulator)
- Create static xcframework
